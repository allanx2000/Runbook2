<Window x:Class="Runbook2.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dlg="clr-namespace:Innouvous.Utils.DialogWindow.Windows;assembly=Innouvous.Utils.DialogWindow.Windows"
        Title="OptionsWindow" Height="100.385" Width="300" SizeToContent="Height">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <dlg:DialogControl Grid.Row="0" Name="OptionsControl"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Command="{Binding SaveChangesCommand}">OK</Button>
            <Button>Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
